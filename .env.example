# Copy to .env. Generate unique passwords; do not commit .env.
POSTGRES_PASSWORD=replace-with-a-long-random-owner-password
READER_PASSWORD=replace-with-a-different-long-random-password
POSTGRES_DB=bg_transparency
POSTGRES_USER=bg_owner
# For local Python only, when using infrastructure/compose.dev.yml:
DATABASE_URL=postgresql+psycopg://bg_owner:replace-with-a-long-random-owner-password@127.0.0.1:55432/bg_transparency
API_INTERNAL_URL=http://127.0.0.1:8000
